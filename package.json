{
  "name": "refrigerator",
  "version": "0.0.1",
  "description": "A small, 0-dependency JS module that can be used to make all property changes on an object throw an error.",
  "main": "src/index",
  "repository": "https://github.com/flotwig/js-refrigerator",
  "author": "Zach Bloomquist <zach@bloomqu.ist>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "yarn build && `npm bin`/jest src/*.spec.js",
    "build": "tsc",
    "prepublishOnly": "yarn test"
  },
  "files": [
    "src/index.js",
    "src/index.ts"
  ],
  "devDependencies": {
    "@types/chai": "^4.2.4",
    "@types/jest": "^24.0.22",
    "chai": "^4.2.0",
    "jest": "^24.9.0",
    "typescript": "^3.7.2"
  }
}
